= hidden_field_tag :direction, params[:direction]
= hidden_field_tag :sort, params[:sort]
= hidden_field_tag :collection, collection_condition
= hidden_field_tag :per_page, per_page

.total_items
  #{@pieces.total_entries} Teile
  %br gefunden:

%table.table
  %tr
    %th= piece_sortable 'name'
    %th= piece_sortable 'collection', 'Kollektion'
    %th= piece_sortable 'color'
    %th= piece_sortable 'fabric', 'Material'
    %th= piece_sortable 'size', 'Grösse'
    %th= piece_sortable 'price', 'Preis'
    %th= piece_sortable 'costs', 'Kosten'
    %th= piece_sortable 'count_produced', 'Produziert'
    %th= piece_sortable 'sold', 'Verkauft'
    %th= piece_sortable 'stock', 'Bestand'

  - @pieces.each do |piece|
    %tr{"data-piece_id" => "#{piece.id}"}
      %td= piece.name
      %td= piece.collection
      %td= piece.color
      %td= piece.fabric
      %td= piece.size
      %td= piece.price
      %td= piece.costs
      %td= piece.count_produced
      %td= piece.sold
      %td= piece.stock

      %td.delete
        - if piece.count_sold == 0
          = link_to piece, :confirm => 'Are you sure?', :method => :delete do
            %i.icon-remove

-# see: http://hungrycoder.xenexbd.com/ruby-on-rails/will-paginate-links-and-twitter-bootstrap.html
= will_paginate @pieces,  :renderer => 'BootstrapPaginationHelper::LinkRenderer'
